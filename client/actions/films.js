import { createAction } from 'redux-actions';
import axios from 'axios';


export const getFilms = createAction('getFilms', () => {
    return 0;
});


export const getFilmsDetails = createAction('getFilmsDetails', (films) => {
  
});
