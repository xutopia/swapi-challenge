import { createAction } from 'redux-actions';
import axios from 'axios';


export const selectCharacter = createAction('selectCharacter', () => {
    return 0;
});


export const clearSelected = createAction('clearSelected');
